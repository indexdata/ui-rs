import React from 'react';
import { Pane } from '@folio/stripes/components';

const ScheduledActionsSettings = () => (
  <Pane defaultWidth="fill" paneTitle="Scheduled Actions" />
);

export default ScheduledActionsSettings;
